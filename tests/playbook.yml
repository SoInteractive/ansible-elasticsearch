---
- hosts: all
  tasks:
    - name: install testing suite
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - iproute 
        # inetutils-ping
      when: ansible_distribution == 'Ubuntu'

- hosts: all
  roles:
    - ansible-elasticsearch
