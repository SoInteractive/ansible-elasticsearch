# {{ ansible_managed }}

cluster.name: {{ elasticsearch_cluster_name }}

node.name: {{ inventory_hostname }}-{{ elasticsearch_instance_name }}

#################################### Paths ####################################

# Path to directory containing configuration (this file and logging.yml):
path.conf: {{ elasticsearch_conf_dir }}

path.data: {{ elasticsearch_data_dirs | join(',') }}

path.work: {{ elasticsearch_work_dir }}

path.logs: {{ elasticsearch_log_dir }}

#################################### Network ##################################

network.host: {{ ansible_default_ipv4.address }}
http.port: {{ elasticsearch_http_port }}

#################################### Discovery ################################
discovery.zen.ping.unicast.hosts:  [{% for item in groups[elasticsearch_hosts_group] %} '{{ hostvars[item]['ansible_default_ipv4']['address'] }}'{% if not loop.last %}, {% endif %} {% endfor %}]

